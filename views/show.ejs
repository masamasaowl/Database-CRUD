<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">

    <link rel="stylesheet" href="/style.css">
    
    <style>
        table, th, td{
            border: 1px solid rgba(0, 0, 0, 0.826);
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>Id</th>
            <th>Username</th>
            <th>Email</th>
        </tr>
        <!-- <tr>
            <td>1</td>
            <td>abc</td>
            <td>abc@gamil.com</td>
        </tr> -->

        <!-- a loop for all the users -->
        <% for (const user of users) {%>
            <tr>
                <td><%= user.id %></td>
                <td><%= user.username %></td>
                <td><%= user.email %></td>

                <!-- a form for to edit the data -->
                 <td>
                    <form method="get" action="/user/<%= user.id %>/edit">
                        <button>Edit</button>
                     </form>
                 </td>

                 <!-- a form for to delete the data -->
                 <td>
                    <form method="post" action="/user/<%= user.id %>/delete?_method=DELETE">
                        <button>Delete</button>
                     </form>
                 </td>
            </tr>

        <% } %>
    </table>
</body>
</html>